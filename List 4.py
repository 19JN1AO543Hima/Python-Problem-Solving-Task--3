integer = 2048

integer = str(integer)

first_digit = int(integer[0])

last_digit = int(integer[-1])

addition = first_digit + last_digit

print("Sum of first and last digit of the integer is: ")

print(addition)
